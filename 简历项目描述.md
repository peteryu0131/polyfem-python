# PolyFEM Python 绑定 — 简历项目描述

以下内容可直接复制到简历中使用，按需选「简短版」或「完整版」。

---

## 一、简短版（项目名 + 3～5 条要点）

**项目名称：** PolyFEM Python 绑定与 API 重设计（polyfem-python）

**要点：**

- 重写 Python 高层 API，并将 C++ 绑定由 **pybind11 迁移至 nanobind**，实现 NumPy 零拷贝与更清晰的接口
- 设计内存优先的 `Result`、显式区分 point_data/cell_data，与 meshio/VTK 生态对接，封装 `CommonContactConfig` 等简化接触与输出配置
- 提供统一 **`solve()` / `batch_solve()`** 接口，支持一键配置求解与批量任务
- 提供 **可微仿真接口**（`solve_differentiable`），基于伴随法与 **PyTorch** 集成，支持形状/材料等参数自动求导，便于与 ML/优化流程对接
- 通过 conda-forge 分发，累计下载量 **30 万+**

**技术栈：** Python · C++ · nanobind · CMake · 有限元(FEM) · 非线性弹性 · 接触力学 · meshio · PyTorch · 可微仿真

---

## 二、完整版（一段话，适合「项目描述」栏）

参与并推进 PolyFEM 官方 Python 绑定的 API 重设计与绑定层升级。**Python 侧**：重写高层 API，包括统一 `solve()` / `batch_solve()` 实现一键配置与批量求解；设计内存优先的 `Result`、point_data/cell_data 分离及 meshio 读写集成；封装 `CommonContactConfig` 等简化接触与 Paraview 输出配置。**C++ 侧**：将 Python 绑定由 pybind11 **迁移至 nanobind**，提升与 NumPy 的零拷贝互操作与可维护性。**可微与 ML**：提供可微仿真接口 `solve_differentiable`，基于伴随法与 PyTorch 集成，支持形状/材料等参数自动求导，便于形状优化、逆问题及物理约束的机器学习流程。项目通过 conda-forge 分发，累计下载量 30 万+。技术栈：Python、C++、nanobind、CMake、meshio、PyTorch。

---

## 三、纯 Bullet 版（只列成就，不写段落）

- 重写 PolyFEM Python API，并将 C++ 绑定由 **pybind11 迁移至 nanobind**
- 设计内存优先 `Result`、point_data/cell_data 分离、meshio 集成及 `CommonContactConfig` 等简化配置
- 提供 **`solve()` / `batch_solve()`**，支持一键求解与批量任务
- 提供 **可微仿真**（`solve_differentiable`）+ **PyTorch** 集成，伴随法求梯度，支持形状/材料等参数，便于 ML/优化
- 通过 conda-forge 分发，累计下载 **30 万+**

---

## 四、英文版（Short，便于英文简历或外企）

**PolyFEM Python Bindings — API Redesign & nanobind Migration**

- Redesigned high-level Python API and **migrated C++ bindings from pybind11 to nanobind** for zero-copy NumPy interop
- Memory-first `Result`, explicit point_data/cell_data, meshio integration, and `CommonContactConfig` for contact/output
- Unified **`solve()` / `batch_solve()`** for one-shot and batch simulations
- **Differentiable simulation** (`solve_differentiable`) with **PyTorch** and adjoint-based gradients (shape/material) for ML and optimization
- Distributed via conda-forge, **300k+** total downloads

**Stack:** Python, C++, nanobind, CMake, FEM, nonlinear elasticity, contact, meshio, PyTorch.

---

按简历篇幅选用：空间少用「一」或「三」，空间多用「二」；需要英文时用「四」。
